import { Meta } from '@storybook/react';
import { ChevronLeft } from '@sbtest/react-lib';

const meta: Meta<typeof ChevronLeft> = {
  title: 'ReactApp/Chevron',
  component: () => <ChevronLeft />,
  args: {},
};

export default meta;

export const Chevron = {
  args: {},
};
