import type { Meta } from '@storybook/react';
import { ChevronLeft } from './chevron';

const meta: Meta<typeof ChevronLeft> = {
  title: 'React Lib/Chevron',
  component: ChevronLeft,
  args: {},
};

export default meta;

export const ChevronStory = {
  args: {},
};
